// Breakpoints (in pixels)
export const BREAKPOINTS = {
	MOBILE: 480,
	TABLET: 768,
	DESKTOP: 1200
} as const;

// Audio visualizer dimensions
export const AUDIO_WAVE = {
	HEIGHT_MOBILE: 80,
	HEIGHT_DESKTOP: 120
} as const;

// Animation durations (in milliseconds)
export const ANIMATION_DURATION = {
	GATE_ROTATION: 800,
	SLIDE_OPEN: 800
} as const;

// Particle system
export const PARTICLES = {
	COUNT_MOBILE: 100,
	COUNT_DESKTOP: 200
} as const;
